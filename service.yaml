apiVersion: v1
kind: Service
metadata:
  name: trend-app-service
spec:
  type: LoadBalancer
  selector:
    app: trend-react-app
  ports:
    - protocol: TCP
      port: 3000         # The port you type in the browser
      targetPort: 3000 # The port inside the container
